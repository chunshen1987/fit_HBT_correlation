flag_1D = 1                       # 1: fit correlation function without cross term HBT radii
                                  # 0: fit correlation function with full set of HBT radii
flag_gsl_fit = 0                  # 1: perform multidimensional fit to the correlation function
                                  # 0: only find the minimum of the chi-sq for correlation function

q_max = 0.2                       # the maximum momentum for q in the fit (GeV)

fit_max_iterations = 1000         # maximum iteration for gaussian fit to get correlation function
fit_tolarence = 1e-6              # tolarence for gaussian fit
